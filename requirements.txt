# Scraper core (MIT-licensed, by Yazan Sharaya).
# Pinned to a specific commit for reproducibility; bump when upstream changes.
# Upstream has no git tags so we pin by SHA (HEAD of main as of 2026-05-19).
upwork_analysis @ git+https://github.com/Yazan-Sharaya/upwork_analysis.git@e3cd8bd4f6c2a5e5374147d611923ef092c3518d

# Scraper transitive deps (also pulled in by upwork_analysis, listed for clarity)
seleniumbase>=4.20
beautifulsoup4>=4.12
scikit-learn>=1.3
scipy>=1.11

# Data
pandas>=2.0
numpy>=1.24

# Geographic enrichment
pycountry>=22.3
pycountry-convert>=0.7

# Google Sheets sink
gspread>=6.0
google-auth>=2.28

# Config / HTTP / progress / logging
python-dotenv>=1.0
requests>=2.31
tqdm>=4.66

# Testing
pytest>=8.0
pytest-mock>=3.12
