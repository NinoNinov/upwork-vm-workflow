# Scraper core (MIT-licensed, by Yazan Sharaya).
# Pinned to our fork's commit SHA. The fork adds card-level location reading
# (fixes stale-panel race), job url + job_id capture, and raw post-time text.
# See UPWORK_ANALYSIS_ROADMAP.md for the full list of fork patches.
upwork_analysis @ git+https://github.com/NinoNinov/upwork_analysis.git@c7021cd966d7ec629e9116da3422b77a0f3cca38

# Scraper transitive deps (also pulled in by upwork_analysis, listed for clarity)
seleniumbase>=4.20
beautifulsoup4>=4.12
scikit-learn>=1.3
scipy>=1.11

# Data
pandas>=2.0
numpy>=1.24

# Geographic enrichment
pycountry>=22.3
pycountry-convert>=0.7

# Google Sheets sink
gspread>=6.0
google-auth>=2.28

# Config / HTTP / progress / logging
python-dotenv>=1.0
requests>=2.31
tqdm>=4.66

# Testing
pytest>=8.0
pytest-mock>=3.12
