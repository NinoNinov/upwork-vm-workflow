# Scraper core (MIT-licensed, by Yazan Sharaya).
# Pinned to a specific commit for reproducibility; bump when upstream releases.
upwork_analysis @ git+https://github.com/Yazan-Sharaya/upwork_analysis.git@v1.0.0

# Scraper transitive deps (also pulled in by upwork_analysis, listed for clarity)
seleniumbase>=4.20
beautifulsoup4>=4.12
scikit-learn>=1.3
scipy>=1.11

# Data
pandas>=2.0
numpy>=1.24

# Geographic enrichment
pycountry>=22.3
pycountry-convert>=0.7

# Google Sheets sink
gspread>=6.0
google-auth>=2.28

# Config / HTTP / progress / logging
python-dotenv>=1.0
requests>=2.31
tqdm>=4.66

# Testing
pytest>=8.0
pytest-mock>=3.12
